<h1> <%= @user.name %></h1>

<h2> Membership Level</h2>
<p>You are currently subscribed at the "<% print @user.role %>" level.</p>

 <%if current_user.role == 'admin' %>
   	<h3> Edit User Membership Level</h3>
   
   	<%= form_for(@user, url: admin_users_edit_path, html: { method: :patch }) do |f| %>  

    <div class="form-group">
    <%= f.label :role %> <%= f.text_field :role, class: 'form-control', placeholder: "Edit Membership Level", autofocus: true %>
     </div>
 
  <div class="form-group"> <%= f.submit "Update", class: 'btn btn-success' %>
    </div>
<% end %>
 <% end %> 
<h2>Comments</h2>

<%= @user.comments.all.each do |comment| %>
	<%= comment %>
<% end %>